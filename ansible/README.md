# Ansible Docker

## Environment:
  * Ubuntu 22.04 LTS
  * Ansible v7.1.0
  * Ansible Core v2.14.1
  * Azure CLI
  * AWS CLI 2.0

## Usage:
* Build image
  ```console
  docker-compose build
  ```
* Run it
  ```console
  docker-compose run --rm ansible
  ```
